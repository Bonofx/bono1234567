# HabitFlow — Demo

This repository contains a Next.js + TypeScript + Tailwind demo of the HabitFlow landing page and dashboard.

Run locally:

1. npm install
2. npm run dev

The app uses localStorage for mock data. Replace /public/hero.svg with a cinematic hero image if you have one.
